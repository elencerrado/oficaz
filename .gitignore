node_modules
node_modules.*
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# 🔒 SECURITY: Environment variables (never commit!)
.env
.env.local
.env.*.local
.env.production
.env.development
.env.prod
.env.test

# 🔒 SECURITY: Sensitive files
*.pem
*.key
*.cert

# 🔒 Android signing keystores (NEVER commit these)
*.keystore
*.jks
android/oficaz-release.keystore

# Temporary scratch scripts
.tmp-*.cjs
tmp/root-scripts/